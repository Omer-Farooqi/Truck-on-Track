Type the following command in your cmd

python truck_manager.py

The file will run and the menu will show up. Make sure the file is in the same directory. If not, you can type cd , plus the directory name and it will change to the directory which contains the file and then run successfully!